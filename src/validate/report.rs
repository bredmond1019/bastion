// Report rendering — greppable per-error lines + summary.
// Implemented fully in Task 4; this stub compiles so Task 1 can wire the shell.

use crate::validate::ValidationError;

/// Render a greppable validation report.
/// Returns a placeholder string until Task 4 fills the implementation.
pub fn render_report(_errors: &[ValidationError], _files_scanned: usize) -> String {
    String::new()
}
