// ratatui render functions: two-pane layout (graph left, node detail right).

use ratatui::Frame;
use crate::monitor::app::App;

pub fn render(_frame: &mut Frame, _app: &App) {
    todo!("Phase 1: left pane = graph canvas, right pane = selected node detail")
}
